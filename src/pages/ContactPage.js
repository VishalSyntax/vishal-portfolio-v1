import React from 'react';
import Contact from '../components/Contact';

const ContactPage = ({ personalInfo }) => {
    return <Contact personalInfo={personalInfo} />;
};

export default ContactPage;
